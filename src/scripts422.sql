CREATE TABLE drivers (id SMALLSERIAL NOT NULL UNIQUE, name VARCHAR(20) PRIMARY KEY, age SMALLINT NOT NULL CHECK (age > 0), license BOOLEAN NOT NULL, car_id SMALLINT NOT NULL REFERENCES cars (id));
CREATE TABLE cars (id SMALLSERIAL PRIMARY KEY, brand VARCHAR(20) NOT NULL, model VARCHAR(20) NOT NULL, cost INTEGER NOT NULL CHECK (cost > 0));